# حلول مقترحة لمشكلة الـ Cache

## المشكلة الأساسية
الصور المستخرجة تُحفظ في `FileSystem.cacheDirectory` الذي يمكن لنظام Android حذفه تلقائياً عند الحاجة للمساحة، مما يجعل التطبيق يعيد الاستخراج من الصفر في كل تشغيل.

---

## الحل 1: الانتقال إلى `documentDirectory` (الأهم)

**الفكرة:**
تغيير مكان حفظ ملفات الصور من:
```
FileSystem.cacheDirectory + 'artwork/art_<hash>.jpg'
```
إلى:
```
FileSystem.documentDirectory + 'artwork/art_<hash>.jpg'
```

**لماذا هذا يحل المشكلة؟**
- `cacheDirectory` → مؤقت، يُمسح من النظام تلقائياً
- `documentDirectory` → دائم، لا يُمسح إلا بحذف التطبيق أو مسح بياناته

**التأثير على التخزين:**
- الصور عبارة عن thumbnails صغيرة (حوالي 50-100KB لكل أغنية)
- لـ 500 أغنية ≈ 25-50MB فقط (مقبول جداً)

**ملاحظة:** يجب إضافة مسح يدوي لهذه الصور من إعدادات التطبيق لاحقاً (زر "مسح الـ Cache").

---

## الحل 2: تنظيف AsyncStorage من المسارات القديمة
عند انطلاق التطبيق، التحقق من كل مسار مُخزن في AsyncStorage:
- إذا كان الملف موجوداً → استخدامه
- إذا كان الملف غير موجود → حذف المفتاح من AsyncStorage فوراً (بدلاً من محاولة الاستخراج مجدداً في نفس الجلسة)

---

## الحل 3: Pre-scan في الخلفية عند فتح التطبيق
بعد تحميل القائمة، بدء عملية scan هادئة في الخلفية تمر على كل أغنية غير مُخزنة وتستخرج صورتها تدريجياً (بأولوية منخفضة لا تؤثر على الـ UI).

---

## ترتيب التطبيق المقترح
1. **الحل 1** أولاً (الأهم والأسرع تأثيراً)
2. **الحل 2** معه مباشرة (تنظيف تلقائي)
3. **الحل 3** لاحقاً كتحسين إضافي
